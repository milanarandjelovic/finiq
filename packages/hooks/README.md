# `@finiq/hooks`

Shared React hooks used across the monorepo. Provides reusable stateful logic that can be consumed by any app or package without duplication. Currently, includes hooks for UI interactions such as month navigation.
