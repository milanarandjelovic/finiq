# `@finiq/schemas`

Shared Zod validation schemas used across the monorepo. Covers the core domain areas of the application including auth, budget, category, goal, profile, settings, and transactions. Keeps validation logic in one place so apps and APIs stay in sync.
