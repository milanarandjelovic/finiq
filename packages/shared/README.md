# `@finiq/shared`

Shared utilities, types, constants, and enums used across the monorepo. Centralizes common building blocks such as application constants, currencies, pagination helpers, date utilities, and domain-level enums. Consumed by both frontend apps and backend services to avoid duplication.
