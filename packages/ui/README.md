# `@finiq/ui`

Shared React component library used across the monorepo. Built on top of Radix UI and Tailwind CSS, it provides a consistent set of accessible components such as buttons, forms, dialogs, tables, and data pickers. Components are added and managed via shadcn.
