# `@finiq/typescript-config`

Shared TypeScript configurations used across the monorepo. Provides presets for base projects, Next.js apps, NestJS apps, and React component libraries. All presets extend a strict base config to ensure consistent type checking standards throughout every package and app.
