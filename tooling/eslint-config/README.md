# `@finiq/eslint-config`

Shared ESLint configurations used across the monorepo. Provides presets for base TypeScript projects, Next.js apps, and internal React packages. Ensures consistent code quality and linting rules throughout all apps and packages.
