# `@finiq/tailwind-config`

Shared Tailwind CSS configuration used across the monorepo. Provides a base stylesheet with design tokens for colors, typography, radius, and shadows covering both light and dark modes. All CSS variables are mapped into Tailwind's theme so they are available as utility classes throughout every app.
