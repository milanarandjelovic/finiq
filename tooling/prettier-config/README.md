# `@finiq/prettier-config`

Shared Prettier configuration used across the monorepo. Includes plugins for automatic import sorting and Tailwind CSS class ordering. Enforces consistent code formatting rules such as single quotes, trailing commas, and no semicolons.
